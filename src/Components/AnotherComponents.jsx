
const AnotherComponents = ({ _id, petName, petImage, maxDonationAmount, donatedAmount, description }) => {
    return (
        <div>
          {/* Other rendering logic using the received props */}
        </div>
      );
    };

export default AnotherComponents;